"""Integrationtests für die Datenpipeline: Loader -> Cleaner -> Formeln."""

def test_pipeline_loader_to_cleaner() -> None:
    """Prüft, ob die Rohdaten aus data_loader vom data_cleaner
    korrekt ins Format (kWh, Zeit) überführt werden."""



def test_cleaned_data_to_formulas() -> None:
    """Prüft, ob bereinigte Daten von den Berechnungsfunktionen
    in formulas.py verarbeitet werden können."""
